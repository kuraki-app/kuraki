# Docs (kept local, not published to the public repo)
docs/

# macOS
.DS_Store

# Go build output
/bin/
/kuraki
/kuraki.exe
*.test
*.out

# Runtime data
/kuraki-data/
*.db
*.db-wal
*.db-shm

# Frontend
web/node_modules/
web/.svelte-kit/
web/build/
web/dist/

# End-to-end run state: the throwaway library the suite seeds, the signed-in
# storage state, traces and reports. All regenerated on every run.
web/e2e/.tmp/
web/playwright-report/
web/test-results/

# Built app packages. The Android APK is operator-supplied and built out of
# band (see README "Android app download") — it is a 51MB artifact, not source.
/prod-builds/
web/assets/
*.apk

# Python tooling cache (web/scripts)
__pycache__/
*.pyc

# Agent execution scratch
.superpowers/

# Editor / tooling
.idea/
.vscode/
*.log

# swag intermediate (only apispec/openapi.json is committed)
internal/httpapi/apispec/swagger.json
.worktrees/

# Wrangler local cache (account id, project bindings) — machine state, not repo state.
.wrangler/
