# Kuraki — https://kuraki.app/
#
# Crawling is closed while the site is served from its temporary
# *.pages.dev address. index.html declares kuraki.app as its canonical
# URL, and that domain does not resolve yet, so anything indexed here
# would point at a dead address and would then have to be migrated.
#
# WHEN kuraki.app IS REGISTERED AND BOUND TO THIS PAGES PROJECT:
# replace the two lines below with
#
#     Allow: /
#
# and add back a Sitemap: line once sitemap.xml exists. Nothing else in
# this file needs to change.

User-agent: *
Disallow: /
